# Project Instructions

## Git Workflow
- Whenever code is updated or fixes are made, create a descriptive git commit.
- Always push changes directly to the remote repository (`git push origin main` or `git push sisir main`) immediately after committing.
