# Project Instructions

## Git Workflow
- Whenever code is updated or fixes are made, create a descriptive git commit.
- Push changes to the remote repository (`git push origin main`). If pushing via the terminal requires GitHub credentials, inform the user or use the configured remote credentials.
